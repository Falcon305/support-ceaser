CLAUDE.md
.env.local
.env
node_modules/
.next/
dist/
*.log
.DS_Store

# fetched PostHog docs/issues — pulled at build time, not vendored into the repo
evals/data/*.fetched.json
evals/data/*.seed.json
